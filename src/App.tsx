import { Stack } from '@mui/material';
import Quiz from './pages/Quiz';

const App = () => {
  return (
    <Stack direction='column' spacing={2}>
      <Quiz />
    </Stack>
  );
};

export default App;
